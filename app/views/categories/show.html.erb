<% if notice %>
  <div class="alert alert-success">
    <%= notice %>
  </div>
<%end%>
<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<%= link_to 'Editar', edit_category_path(@category), :class => "btn btn-default" %> 
<%= link_to 'Atrás', categories_path, :class => "btn btn-default"  %>
